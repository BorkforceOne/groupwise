import { MatchStatusPipe } from './match-status.pipe';

describe('MatchStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new MatchStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
